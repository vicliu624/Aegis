# Hello App Example

This example corresponds to the runtime-built `demo_hello` app in `/apps/demo_hello`.

It demonstrates:

- app entry through the Aegis runtime contract
- UI root creation through Host API
- notification publication through the system-owned notification service
- clean return to shell after session teardown
